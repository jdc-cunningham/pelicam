import './ButtonBindings.scss';

const ButtonBindings = (props) => {
  return (
    <div className="App__right-button-bindings">
      <h2>Button bindings</h2>
    </div>
  );
};

export default ButtonBindings;
